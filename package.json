{
  "name": "nutribudy-bot-worker",
  "version": "1.0.0",
  "description": "AI-driven nutrition bot using Cloudflare Workers and AI models",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "dependencies": {}
}